# Collector
numpy>=1.26.0
scipy>=1.11.0
pyalsaaudio>=0.10.0
# tflite-runtime: use on Python 3.11 Pi (piwheels). No 3.13 wheel on PyPI.
# ai-edge-litert: Google's LiteRT successor — try on Python 3.12/3.13:
#   pip install ai-edge-litert
# If both fail, use a Python 3.11 venv: python3.11 -m venv .venv

# Web UI
fastapi>=0.111.0
uvicorn>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
psutil>=5.9.0
python-dotenv>=1.0.0
itsdangerous>=2.1.0
