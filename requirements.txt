# Collector
numpy>=1.26.0
scipy>=1.11.0
pyalsaaudio>=0.10.0
# tflite-runtime must be installed separately on Pi (no Python 3.13 wheel on pypi/piwheels).
# See: https://github.com/google-coral/pycoral/releases for arm64 wheels,
# or install via: pip install tflite-runtime --extra-index-url https://google-coral.github.io/py-pkgs-coral-apt/simple/

# Web UI
fastapi>=0.111.0
uvicorn>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
psutil>=5.9.0
python-dotenv>=1.0.0
itsdangerous>=2.1.0
